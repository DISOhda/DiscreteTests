% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permutation.r
\name{perm_test_pv}
\alias{perm_test_pv}
\title{Permutation Test}
\usage{
perm_test_pv(
  x,
  y,
  statistic = c("diff_mean", "diff_median", "diff_t", "diff_hl", "ratio_var", "ratio_sd"),
  mu = NULL,
  alternative = "two.sided",
  exact = NULL,
  max_exact_combs = 10L^7,
  MC_sims = 10000L,
  seed = NULL,
  simple_output = FALSE
)
}
\arguments{
\item{x}{numerical vector or list of numerical vectors of
observations in the first group.}

\item{y}{numerical vector or list of numerical vectors of
observations in the second group.}

\item{statistic}{single character giving the type of test statistic
to be used; must be one of \code{"diff_mean"},
\code{"diff_median"}, \code{"diff_t"}, \code{"diff_hl"},
\code{"ratio_var"} or \code{"ratio_sd"} (see Details).}

\item{mu}{numerical vector giving the hypothesised values
under the null; if \code{mu == NULL} (the default) it
is set to 0 for test statistics that are based on
differences (\code{diff_}\emph{) or 1 for ratio-based tests
statistics (\code{ratio_}}).}

\item{alternative}{character vector that indicates the alternative hypotheses; each value must be one of \code{"two.sided"} (the default), \code{"less"} or \code{"greater"}.}

\item{exact}{logical value that indicates whether \eqn{p}-values
are to be calculated by exact computation (\code{TRUE})
or by simulation (\code{FALSE}); if \code{NULL} (the default)
exact computation is performed if the number of
possible sample combinations (see Details) is below
or equal to \code{max_exact_combs}; otherwise the
respective \eqn{p}-values are obtained by
simulation.}

\item{max_exact_combs}{maximum number of allowed combinations for exact
computation of the permutation distribution (see
details).}

\item{MC_sims}{positive integer or \code{NULL}. The number of Monte
Carlo permutations to draw when \code{exact = FALSE}.
Ignored when \code{exact = TRUE}. Default is \code{10000L}.}

\item{seed}{single integer or \code{NULL}. Random seed passed to
\code{\link[=set.seed]{set.seed()}} before Monte Carlo sampling to ensure
reproducibility. Use \code{NULL} to skip seed setting.
Ignored when \code{exact = TRUE}.}

\item{simple_output, simple.output}{logical value that indicates whether an R6 class object, including the tests' parameters and support sets, i.e. all observable p-values under each null hypothesis, is to be returned (see below).}
}
\value{
If \code{simple.output = TRUE}, a vector of computed p-values is returned.
Otherwise, the output is a \code{\link{DiscreteTestResults}} R6 class object, which
also includes the p-value supports and testing parameters. These have to be
accessed by public methods, e.g. \verb{$get_pvalues()}.
}
\description{
\code{perm_test_pv()} performs a permutation test for the comparison of two
independent samples using a user-supplied test statistic. It is vectorised
over multiple test problems: by passing lists of sample vectors for \code{x} and
\code{y}, several tests can be evaluated simultaneously. The function can compute
exact \emph{p}-values (by enumerating all permutations) or approximate \emph{p}-values
via Monte Carlo sampling. It is capable of returning the discrete \emph{p}-value
supports, i.e. all observable \emph{p}-values under the null hypothesis.
}
\details{
Under the null hypothesis of exchangeability (i.e. both samples come from
the same distribution), all \eqn{\binom{n_x + n_y}{n_x}} partitions of the
pooled sample into groups of sizes \eqn{n_x} and \eqn{n_y} are equally
likely. \code{perm_test_pv()} exploits this by:

\enumerate{
\item Computing the observed test statistic \eqn{T_\text{obs}} from
\code{x} and \code{y} using the function selected by \code{statistic} (see the
\emph{Test Statistics} section below).
\item Enumerating (if \code{exact = TRUE}) or randomly sampling (if
\code{exact = FALSE}) permutations of the pooled sample.
\item Evaluating the selected test statistic on each permuted split.
\item Deriving the \emph{p}-value as the fraction of permutation statistics
that are at least as extreme as \eqn{T_\text{obs}}, where \emph{extreme}
is defined by \code{alternative}:
\describe{
\item{\code{"less"}}{fraction with permuted statistic
\eqn{\le T_\text{obs}}}
\item{\code{"greater"}}{fraction with permuted statistic
\eqn{\ge T_\text{obs}}}
\item{\code{"two.sided"}}{fraction with
\eqn{|\text{permuted statistic}| \ge |T_\text{obs}|}}
}
}

Exact computation is only feasible for small pooled samples; the total
number of permutations grows as \eqn{\binom{n_x + n_y}{n_x}}. If
\code{exact = NULL}, exact computation is applied when the number of computations
is below or equal to \code{max_exact_combs} (default \eqn{20{,}000}). Otherwise,
the distribution of the test statistics is approximated by Monte Carlo
simulation. When \code{exact = TRUE} and the number of permutations exceeds
\code{max_exact_combs}, an error is raised. Set \code{exact = FALSE} to use Monte Carlo
sampling in such cases.

The parameters \code{x}, \code{y}, \code{alternative}, and \code{MC_sims} are vectorised via
lists: if \code{x} and \code{y} are lists of the same length, each pair
\verb{(x[[i]], y[[i]])} defines one test. Scalars and single vectors are
recycled to the required length.
}
\examples{
set.seed(42)
x1 <- rnorm(8)
y1 <- rnorm(8, mean = 1)

# Exact two-sided p-value (default: difference of means)
results_ex <- perm_test_pv(x1, y1)
print(results_ex)
results_ex$get_pvalues()
results_ex$get_pvalue_supports()

# Monte Carlo approximation with a custom statistic (median difference)
results_mc <- perm_test_pv(
  x1, y1,
  stat_fun = function(x, y) median(x) - median(y),
  exact = FALSE, MC_sims = 9999L, seed = 1L
)
results_mc$get_pvalues()

# Multiple tests simultaneously
xs <- list(rnorm(6), rnorm(5, 1))
ys <- list(rnorm(6, 1), rnorm(5, 2))
results_v <- perm_test_pv(xs, ys, exact = FALSE, MC_sims = 4999L, seed = 7L)
results_v$get_pvalues()

}
\references{
Pitman, E. J. G. (1937). Significance tests which may be applied to
samples from any populations. \emph{Supplement to the Journal of the Royal
Statistical Society}, \strong{4}(1), pp. 119–130. \doi{10.2307/2984124}

Good, P. (2000). \emph{Permutation Tests: A Practical Guide to Resampling
Methods for Testing Hypotheses}. Second Edition. New York: Springer.
\doi{10.1007/978-1-4757-3235-1}
}
\seealso{
\code{\link[=mann_whitney_test_pv]{mann_whitney_test_pv()}}, \code{\link[stats:wilcox.test]{stats::wilcox.test()}}
}
