#' @return
#' If `simple.output = TRUE`, a vector of computed p-values is returned.
#' Otherwise, the output is a [`DiscreteTestResults`] R6 class object. Results are
#' instead being accessed by public methods, e.g. `$get_pvalues()`.
