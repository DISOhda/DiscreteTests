#' <%=ifelse(exists("alternative") && alternative,     "@param alternative                   character vector that indicates the alternative hypotheses; each value must be one of `\"two.sided\"` (the default), `\"less\"` or `\"greater\"`.", "") %>
#' <%=ifelse(exists("ts_method") && ts_method,         "@param ts_method,ts.method           single character string that indicates the two-sided p-value computation method (if any value in `alternative` equals `\"two.sided\"`) and must be one of `\"minlike\"` (the default), `\"blaker\"`, `\"absdist\"` or `\"central\"` (see details). Ignored, if `exact = FALSE`.", "") %>
#' <%=ifelse(exists("exact") && exact,                 "@param exact                         logical value that indicates whether p-values are to be calculated by exact computation (`exact = TRUE`; the default) or by a continuous approximation.", "") %>
#' <%=ifelse(exists("correct") && correct,             "@param correct                       logical value that indicates if a continuity correction is to be applied (`correct = TRUE`; the default) or not. Ignored, if `exact = TRUE`.", "") %>
#' <%=ifelse(exists("simple_output") && simple_output, "@param simple_output,simple.output   logical value that indicates whether an R6 class object, including the tests' parameters and support sets, i.e. all observable p-values under each null hypothesis, is to be returned (see below).", "") %>
